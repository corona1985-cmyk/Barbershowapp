{
  "name": "barbershow-functions",
  "version": "1.0.0",
  "description": "Cloud Functions para BarberShow (envío WhatsApp vía Twilio API REST)",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "firebase-admin": "12.0.0",
    "firebase-functions": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "5.3.3"
  },
  "private": true
}
